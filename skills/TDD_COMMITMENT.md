# TDD 承诺与检查清单

## 当前状态承认
- `freqtrade-hyperliquid`: ❌ 未遵循 TDD（先实现后测试）- 标记为技术债务
- 未来所有 Skill: ✅ 必须严格遵守 TDD

## TDD 三定律（必须遵守）

### 定律一：先写测试
```
红色阶段 (RED)
├── 1. 写测试用例
├── 2. 运行测试 → 必须失败
└── 3. 确认失败原因符合预期
```

### 定律二：让测试通过
```
绿色阶段 (GREEN)
├── 1. 写最小实现代码
├── 2. 运行测试 → 必须通过
└── 3. 不添加多余功能
```

### 定律三：重构
```
蓝色阶段 (BLUE)
├── 1. 在测试通过前提下重构
├── 2. 提高代码质量
├── 3. 保持测试通过
└── 4. git commit
```

## 新 Skill TDD 检查清单

### ✅ 开始开发前
- [ ] 创建技能目录结构
- [ ] 创建 `SKILL.md` 定义功能和接口
- [ ] 创建 `tests/` 目录
- [ ] 创建测试文件（空）

### ✅ 开发阶段（循环执行）
- [ ] **步骤1**: 写一个测试用例 → 运行 → 确认失败 ❌
- [ ] **步骤2**: 写最小实现 → 运行 → 确认通过 ✅
- [ ] **步骤3**: 重构代码 → 运行 → 确认通过 ✅
- [ ] **步骤4**: `git commit -m "[TDD] 功能名: 测试+实现"`
- [ ] 重复直到功能完成

### ✅ 完成前
- [ ] 所有测试通过
- [ ] 测试覆盖率 > 80%
- [ ] 更新 `SKILL.md` 使用文档
- [ ] 运行 `python -m pytest tests/ -v`

## 违规后果
如果在开发过程中发现自己：
- 先写了实现再补测试 → **立即停止，删除代码，从测试重新开始**
- 写了过多实现 → **回滚到上一个通过的提交，重新 TDD**

## 示例工作流

```bash
# 1. 创建测试文件 (RED)
cat > tests/test_example.py << 'EOF'
def test_feature_x():
    obj = MyClass()
    result = obj.feature_x()
    assert result == expected
EOF

python -m pytest tests/test_example.py -v  # 必须失败 ❌

# 2. 最小实现 (GREEN)
cat > example.py << 'EOF'
class MyClass:
    def feature_x(self):
        return expected  # 硬编码，让测试通过
EOF

python -m pytest tests/test_example.py -v  # 必须通过 ✅

# 3. 重构 (BLUE)
# 优化代码，添加真实逻辑
# 保持测试通过

# 4. 提交
git add .
git commit -m "[TDD] Add feature_x with tests"
```

## 记录

| 日期 | Skill | TDD 状态 | 备注 |
|------|-------|----------|------|
| 2026-01-31 | trade-analyzer | ✅ | 12/12 测试通过 |
| 2026-01-31 | social-harvester | ✅ | 18/18 测试通过 |
| 2026-01-31 | freqtrade-hyperliquid | ❌ | 技术债务，20/20 测试通过但先实现后测试 |
| 2026-01-31+ | 未来所有 Skill | 🎯 | 严格遵守 TDD |

---
**承诺**: 我将严格遵守 TDD 流程，确保代码质量和可维护性。
